import { motion } from 'framer-motion';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faScroll, faHeart } from '@fortawesome/pro-solid-svg-icons';
import { faXTwitter, faGithub } from '@fortawesome/pro-brands-svg-icons';
import SocialLink from './SocialLink.jsx';

const currentYear = new Date().getFullYear();

export default function Footer() {
  return (
    <motion.footer
      className="w-full py-12 text-center text-sm text-muted bg-gradient-to-b from-transparent to-accent/10 mt-16 shadow-inner"
      initial={{ opacity: 0, y: 24 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.6, ease: 'easeOut' }}
    >
      <p className="flex justify-center items-center gap-3 text-lg font-medium tracking-tight">
        <FontAwesomeIcon icon={faScroll} className="text-accent animate-pulse" />
        © {currentYear} Diggity Dog. All rights reserved.
      </p>

      <div className="mt-6 flex justify-center gap-6 items-center">
        <FontAwesomeIcon icon={faHeart} className="text-accent animate-bounce" size="2x" />
        <SocialLink
          href="https://x.com/DiggityDogHunt"
          label="Follow Diggity Dog on X"
          icon={faXTwitter}
        />
        <SocialLink
          href="https://github.com/DiggityDog/beepanddig"
          label="Beep & Dig GitHub Repo"
          icon={faGithub}
        />
      </div>
    </motion.footer>
  );
}
